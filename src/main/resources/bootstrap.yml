spring:
  application:
    name: banking-witdrawal-service
  cloud:
    config:
      uri: http://localhost:8090
      username: ${security_name}
      password: ${security_password}
  data:
    mongodb:
      uri: mongodb+srv://jespinoza:jest444@jespinoza.is5j4.mongodb.net/witdrawal_data